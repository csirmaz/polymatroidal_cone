

triads -- generate points from all closed S sets

convexhull_iter.py -- use points that satisfy the necessary condition (NC) to calculate convex hulls

convexhull_gen.py -- use points generated using the full-row rule to calculate convex hulls




gcc -lm triads.c -o triads

./triads 12 | python convexhull_iter.py > combined_report.txt
                SET CREATE_SCAD
                SET CREATE_POINT_DATA
                -> hull*.scad
                -> pointdata
                
python process_pointdata.py < pointdata > processeddata
    SET OUTPUT_UP_TO_ITER
    SET OUTPUT_LAST_ITER_ONLY

    <- processeddata (SET DATAFILE)
python train_model.py
    -> model_data (SET OUTFILE)
   
    <- model_data (SET MODELFILE)
    <- processeddata (SET DATAFILE)
python interpret_model.py

